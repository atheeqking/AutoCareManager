import { apiClient } from './apiClient'
import type { Customer, CustomerInput, Vehicle, VehicleInput } from '../types/domain'
export const customerService = { list: (search = '') => apiClient.get<Customer[]>('/customers', { params: search ? { search } : {} }), mine: () => apiClient.get<Customer>('/customers/me'), get: (id: string) => apiClient.get<Customer>(`/customers/${id}`), create: (input: CustomerInput) => apiClient.post<Customer>('/customers', input), update: (id: string, input: CustomerInput) => apiClient.put<Customer>(`/customers/${id}`, input), vehicles: (id: string) => apiClient.get<Vehicle[]>(`/customers/${id}/vehicles`), createVehicle: (id: string, input: VehicleInput) => apiClient.post<Vehicle>(`/customers/${id}/vehicles`, input), vehicle: (id: string) => apiClient.get<Vehicle>(`/vehicles/${id}`), history: (id: string) => apiClient.get<any[]>(`/vehicles/${id}/service-history`), updateVehicle: (id: string, input: VehicleInput) => apiClient.put<Vehicle>(`/vehicles/${id}`, input) }
