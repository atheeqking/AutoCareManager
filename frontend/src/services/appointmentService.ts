import { apiClient } from './apiClient'
export type Appointment={appointmentId:string;customerId:string;customerName:string;phone:string;vehicleId:string;vehicle:string;licensePlate:string;requestedDate:string;requestedTime:string;serviceType:string;issueDescription:string|null;priority:string;status:string}
export const appointmentService={list:(status?:string)=>apiClient.get<Appointment[]>('/appointments',{params:status?{status}:{}}),create:(x:object)=>apiClient.post<Appointment>('/appointments',x),confirm:(id:string)=>apiClient.post(`/appointments/${id}/confirm`),cancel:(id:string)=>apiClient.post(`/appointments/${id}/cancel`),noShow:(id:string)=>apiClient.post(`/appointments/${id}/no-show`),checkIn:(id:string)=>apiClient.post(`/appointments/${id}/check-in`),walkIn:(x:object)=>apiClient.post('/service-cases/walk-ins',x)}
